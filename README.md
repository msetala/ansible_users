# user management with ansible

User creation and management made simple with ansible. This ansible-playbook covers user generation, setting up home & work environment, as well as managing ssh keys and permissions. All you have to do is run the playbook!
<br></br>
Note: to create an encrypted password yourself, make sure to replace the hashed password with your own using ansible-vault.
